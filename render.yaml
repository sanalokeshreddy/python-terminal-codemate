services:
  - type: web
    name: python-terminal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_interface.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16